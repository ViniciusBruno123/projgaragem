venv/
__pycache__/
*.pyc
db.sqlite3
media/
.env
staticfiles/

# binários compilados de skills (específicos de plataforma, reinstaláveis via `npx impeccable install`)
.claude/skills/*/scripts/bin/

test-artifacts/

# cache local do hook do impeccable (por sessão) e config por desenvolvedor — não são compartilhados;
# .impeccable/config.json (as ignore-rules combinadas) é a única parte deste diretório versionada.
.impeccable/hook.cache.json
.impeccable/config.local.json
design_assets_demo/

# grafo de conhecimento do graphify (regenerável: graphify extract . --code-only)
graphify-out/
